/**
 * 世界接口
 */
export class World
{
    /**
     * 添加一个刚体方块
     * @param {any} sn 方块id
     * @param {any} x x坐标
     * @param {any} y y坐标
     * @param {any} z z坐标
     * @param {any} [mass] 质量
     * @param {any} [sx] x缩放
     * @param {any} [sy] y缩放
     * @param {any} [sz] z缩放
     */
    addCube(sn, x, y, z, mass = 0, sx = 1, sy = 1, sz = 1)
    {
    }
}